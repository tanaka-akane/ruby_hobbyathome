<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>おうちシュミ</title>
    <meta name="description" content="おうち時間を充実させるシュミを見つけよう">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= favicon_link_tag('favicon.ico') %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v6.1.2/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header> 
      <%= link_to image_tag('rogoheddertouka'), '/items' , class: 'logopc' %>
      <%= link_to image_tag('rogohedderres'), '/items' , class: 'logosp' %>
      <div class="header-list">
        <% if user_signed_in? %>
        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
        <% else %>
        <%= link_to "ログイン", new_user_session_path %>
        <%= link_to "新規登録", new_user_registration_path %>
        <% end %>
        <% if user_signed_in? %>
        <%= link_to "マイページへ", user_path(current_user.id) %>
        <% end %>
        <% if user_signed_in? && current_user.admin? %>
        <%= link_to "新規投稿ページへ", new_admin_item_path %>
        <% end %>
      </div>
    </header>
    
    
    <main>
      <%= yield %>
    </main>
  
    <footer>
        <p>Copyright © 2022 Akane Tanaka All Rights Reserved.</p>
    </footer>
  </body>
</html>